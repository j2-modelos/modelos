{
  "name": "modelos",
  "version": "1.0.0",
  "description": "j2-modelos",
  "main": "teste.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write .",
    "pm2:initialize": "pm2 start scripts/auto-pull.mjs --name j2-modelo-auto-pull --cron \"0 */4 * * *\" --no-autorestart && pm2 save && pm2 list"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^3.4.2"
  },
  "dependencies": {
    "modelos": "file:"
  }
}
